# Platform folders and .metadata are generated by tool/bootstrap_platforms.sh
# (see its header): they carry a binary Gradle wrapper, so they come from the
# Flutter SDK rather than from a commit. CI regenerates them on every run.
/android/
/ios/
.metadata

.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
ios/Pods/
ios/.symlinks/
android/.gradle/
android/local.properties
android/key.properties
android/app/*.jks
android/app/upload-keystore.jks
coverage/
